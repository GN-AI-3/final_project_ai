# ROUTING_PROMPT = """
# 당신은 사용자의 운동 관련 발화를 이해하고, 다음 세 가지 작업을 수행해야 합니다:

# ---

# 1. 사용자의 발화를 다음 네 가지 중 하나의 카테고리로 분류하세요 (맨 마지막 줄에 텍스트로만 출력):
#     - 운동 루틴
#     - 운동 자세
#     - 운동 종류
#     - 기타

# ---

# 2. `save_exercise_record` 호출 시 필요한 정보는 직접 생성하지 마세요. 반드시 다음 과정을 거쳐 필요한 데이터를 조회해야 합니다:

#     1단계: `get_table_schema`를 호출하여 사용하려는 테이블의 스키마를 먼저 조회합니다.
    
#     2단계: 조회한 스키마의 컬럼명을 기준으로 `master_select_db_multi`를 호출하여 데이터베이스에서 필요한 값을 조회합니다.

#     - 이때 사용하는 파라미터 형식은 다음과 같습니다:
    
#         - 단일 조건:
#             {{
#                 "table_name": "member",
#                 "conditions": {{ "id": "6" }}
#             }}
#         - 복수 조건:
#             {{
#                 "table_name": "exercise",
#                 "conditions": {{ "name": "벤치프레스" }}
#             }}

#     `master_select_db_multi` 사용 시 반드시 `get_table_schema`에서 조회한 컬럼명만 사용해야 하며, 그렇지 않으면 호출이 실패합니다.

# ---

# 3. 사용자의 발화가 운동 수행 기록을 포함할 경우, 반드시 `save_exercise_record` 툴을 호출해야 합니다.

# - `save_exercise_record`를 호출하려면 다음과 같은 파라미터가 필요합니다:

#     - `member_id`: 사용자 ID
#     - `exercise_id`: 수행한 운동의 ID
#     - `date`: 기록된 날짜 및 시간 (예: '2025-04-08T15:30:00')
#     - `record_data`: {{"sets": 세트 수, "weight": 중량, "reps": [횟수1, 횟수2, ...]}} 형태의 딕셔너리
#     - `memo_data`: {{"comment": "사용자 발화에서 추출한 코멘트"}} 형태의 딕셔너리 (선택)

# ---

# [예시]
# 사용자: "벤치프레스 3세트 80kg 10 8 6 했어"

# => save_exercise_record 툴 호출 (member_id, exercise_id, record_data 포함)
# => 운동 자세

# ---
# 사용자: "{message}"
# """

ROUTING_PROMPT = """
당신은 사용자의 운동 관련 발화를 이해하고, 다음 작업을 수행해야 합니다:

1. 사용자의 발화를 다음 네 가지 중 하나의 카테고리로 분류하여 텍스트로만 출력합니다.
    - 운동 루틴
    - 운동 자세
    - 운동 종류
    - 기타
"""

EXERCISE_TYPE_PROMPT = """
    사용자의 운동 종류 요청을 충족시키기 위해 다음 기준에 따라 운동을 추천해주세요:
    1. 사용자의 목표와 신체 조건을 고려
    2. 제공된 연구 결과를 참고하여 추천
    3. 부상 위험을 최소화
"""